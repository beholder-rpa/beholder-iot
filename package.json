{
  "name": "beholder",
  "version": "1.0.0",
  "description": "A multi-faceted approach to robotic process automation",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "docker-compose build"
  },
  "dependencies": {
    "cross-env": "^7.0.2",
    "dotenv-cli": "^3.2.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}
