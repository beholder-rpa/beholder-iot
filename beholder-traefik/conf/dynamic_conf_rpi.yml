# Default TLS Certs
tls:
  certificates:
    - certFile: /etc/traefik/certs/{{ env "rpi_hostname" }}.crt
      keyFile: /etc/traefik/certs/{{ env "rpi_hostname" }}.key
    - certFile: /etc/traefik/certs/nexus.{{ env "rpi_hostname" }}.crt
      keyFile: /etc/traefik/certs/nexus.{{ env "rpi_hostname" }}.key
    - certFile: /etc/traefik/certs/graphana.{{ env "rpi_hostname" }}.crt
      keyFile: /etc/traefik/certs/graphana.{{ env "rpi_hostname" }}.key
